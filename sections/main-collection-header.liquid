<div class="wrapper pt-48 pb-12 grid lg:grid-cols-5 gap-4">
    <h1 class="h3">
      {{ collection.title }}
    </h1>
    
    {% if collection.description != blank %}
      <div class="lg:col-span-2">
        {{ collection.description }}
      </div>
    {% endif %}
</div>

{% schema %}
{
  "name": "Collection header",
  "tag": "section",
}
{% endschema %}
